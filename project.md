# 项目综合需求与技术架构文档 (PRD & TAD)
**项目代号**：Distilled Memory (网页端数字幽灵)
**核心受众**：刘总（玩家）
**技术路线**：前端原生 SPA (HTML/CSS/JS) + Vercel Serverless (Node.js) + Qwen LLM API (基于 Fat Prompt 大上下文架构)

---

## 第一部分：产品需求说明 (PRD)

### 1. 核心概念
这是一个披着“复古终端聊天室”外衣的互动文字/元宇宙游戏。玩家将与一个自称处在“高中时期”的数字分身进行对话。系统底层没有死板的规则树，AI 的所有行为、记忆和语气，全部由一段内置的 3000 字记忆设定文档和强大的大模型上下文能力驱动。

### 2. 视觉与 UI 表现 (Frontend UI)
* **黑客/复古终端美学**：纯黑背景 (`#000000`)，单色等宽字体（推荐复古暗绿色或纯白色）。
* **主聊天流**：居中显示的对话记录，AI 端的回复必须具有“打字机效果”（逐字渲染）。
* **极简输入区**：页面底部固定一个无边框、无背景色的输入框，仅保留闪烁的光标。
* **Meta 系统日志 (角落彩蛋)**：屏幕右上角存在一个半透明、极小字号的 `div`。随着聊天进行，随机滚动一些隐喻“系统底层/真实技术栈”的伪代码日志。

### 3. 核心交互机制 (Meta Interactions)
* **交互 A：焦点劫持 (Focus Hijacking)**
    * 当玩家**第一次**尝试点击底部输入框准备打字时，前端强行拦截该 `focus` 事件。
    * 输入框变灰/禁用 3 秒，同时 AI 在聊天流中主动抢话（例如：“你刚才光标停了 2 秒，你想说什么？”）。
* **交互 B：王家卫式电影节点 (Cinematic Overlay)**
    * 这部分不走 AI 生成，纯前端控制。
    * 当触发特定条件（后续可通过关键字或特定按钮触发），弹出一个全屏黑色的高层级遮罩。以繁体明朝体逐字浮现一段极具电影感、带有时间/距离隐喻的字幕文字。播放完毕后自动淡出，回到聊天流。

### 4. 游戏机制：飞花令
* 无硬编码的复杂判定树，规则约束交由大模型处理。
* 玩家输入古诗词或包含特定字眼的句子时，LLM 需根据其内置的 System Prompt 规则，自动接出符合要求的真实古诗词，并附带高中生视角的吐槽。

---

## 第二部分：技术架构设计 (TAD)

### 1. 目录结构规划
```text
/
├── index.html          # 单页面入口
├── styles.css          # 全局样式与终端特效
├── js/
│   ├── main.js         # 初始化与主循环
│   ├── ui.js           # DOM操作、打字机特效、全屏字幕控制
│   ├── event.js        # 输入监听、焦点劫持逻辑
│   └── api.js          # Fetch 请求封装
├── api/
│   └── chat.js         # Serverless 后端入口 (Node.js)
├── .env                # 本地环境变量 (配置 API_KEY)
└── package.json        # 依赖描述 (主要用于后端发版)
2. 前端核心逻辑 (Vanilla JS)
状态维护：前端需在内存中维护一个 messages 数组，保存最近的 10 轮历史对话（格式：[{role: 'user', content: '...'}, {role: 'assistant', content: '...'}]），每次发送新消息时，将整个数组发给后端。

打字机队列：AI 返回消息后，不能直接 innerHTML，必须使用 requestAnimationFrame 或 setInterval 将字符串推入 DOM，期间需锁定输入框防止玩家乱敲。

3. 后端架构与 Fat Prompt 注入 (Serverless)
后端 api/chat.js 极其轻量，作为安全中转站，核心任务是组装 XML 格式的 System Prompt 并调用千问 API。

Payload 处理流程：

接收前端传来的 messages 数组。

读取本地预设的 SYSTEM_PROMPT 常量（包含角色设定、3000 字共同记忆、飞花令规则）。

将 SYSTEM_PROMPT 作为 role: "system" 插入到 messages 数组的第 0 位。

携带 QWEN_API_KEY 发起对大模型服务器的请求。

（重要）为了保持 AI 语气的高稳定性，调用 LLM API 时需设置参数：temperature: 0.3。

将生成的文本返回给前端。

4. System Prompt 结构规范 (供后续填入文档用)
后端代码中请预留以下 XML 结构的 Prompt 模板：

XML
<role>
你现在的身份是我本人。你正处在高中时期。你的聊天对象是我的死党“刘总”。语气随意、自然，带点高中生的吐槽。绝对不能表现出 AI 的痕迹。
</role>

<memory_core>
[后续将在此处填入 3000 字文档]
</memory_core>

<feihualing_rules>
如果刘总主动发送了包含“花、月、春、风”等意象的诗句，你必须回复一句包含该关键字的、真实的初高中必背古诗词。绝对不能自己编造。回复完诗句后，加上一句你自己的口语吐槽。
</feihualing_rules>

<output_constraints>
每次回复字数控制在 40 字以内。结合记忆自然流露，切忌生硬罗列。
</output_constraints>
第三部分：给 AI 编程助手的实施指令 (Action Items)
作为我的 AI 编程助手，请根据上述文档，开始第一阶段的零到一构建。请勿使用 React/Vue 等框架，严格使用纯原生前端技术，保持代码的白盒化和极简。

请依次输出以下代码：

前端基础结构：生成 index.html 以及配套的 styles.css，实现复古纯黑终端风格、右上角的半透明 system-log 滚动区域，以及底部的无边框输入框。

前端交互逻辑：编写 js/ui.js 和 js/event.js，实现基础的打字机特效函数，并完整实现“焦点劫持”逻辑（当玩家第一次 focus 输入框时，禁用它 3 秒，并在屏幕上打印一句预设的 AI 搭话）。

前后端通信封装：编写 js/api.js，维护包含最近 10 条历史记录的数组，并实现向 /api/chat 的 POST 请求逻辑。

后端 Serverless 骨架：编写 api/chat.js (Node.js)，使用原生 fetch 处理接收到的对话历史，将 XML 结构的预设 System Prompt 插入数组头部，组装千问的大模型请求，并返回数据。